{
  "name": "second-brain",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Local-first, offline-capable note-capture PWA with Firebase sync and auto-filing.",
  "scripts": {
    "dev": "vite",
    "host": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy"
  },
  "dependencies": {
    "firebase": "^12.14.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.3.3",
    "vite-plugin-pwa": "^0.20.0"
  }
}
